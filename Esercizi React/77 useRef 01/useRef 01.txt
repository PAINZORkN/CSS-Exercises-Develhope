Modify the `Login` component from useState 02 to so that the `username` input is automatically focused when the component renders, by creating a `ref` with `useRef` and attaching it to the username input;